from __future__ import annotations

# Single source of truth for package builds. Runtime reads installed package
# metadata first and falls back to this value only when running from source.
__version__ = "1.0.5"
